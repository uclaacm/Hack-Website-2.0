const express = require('express');
let router = express.Router();

router.get('/', (req, res) => {
	/**
	 * TODO: implement getting the scoreboard
	 * Return a sorted array of teams in descending order by total points
	 * Each object should be the full team object
	 */
});

module.exports = { router };
