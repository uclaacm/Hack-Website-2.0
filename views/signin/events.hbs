<!DOCTYPE html>
<html>
    <head>
        <title>ACM | Sign In | Events</title>
        <link href="https://fonts.googleapis.com/css?family=Montserrat:700|Open+Sans:300,400,400i|Trirong" rel="stylesheet" />
        <link rel="stylesheet" type="text/css" href="/css/signin/index.css" />
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body> 
        <div id="main">
            <h1>Sign In</h1>
            {{#if events}}
                <p>Select an event to sign in to.</p>
                <div class="events">
                    {{#each events}}
                        <a class="nostyle" href="/signin/{{ this.code }}">
                            <button class="main event">
                                <span class="event-title">{{ this.title }}</span>
                                <span class="event-org">({{ this.organization }})</span>
                            </button>
                        </a>
                    {{/each}}
                </div>
            {{else}}
                <p>There are no events going on right now.</p>
            {{/if}}
            
        </div>
    </body>
</html>